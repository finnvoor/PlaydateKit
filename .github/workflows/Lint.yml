name: Lint
on:
  push:
    branches-ignore:
      - main

jobs:
  Lint:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v3
      - name: SwiftFormat
        run: |
          swiftformat --lint . --swiftversion 6.0 --reporter github-actions-log
